export const BREAKPOINTS = {
    DESKTOP: 768,
    TABLET: 480,
} as const;

/** Web rail in `app/_layout.tsx` — use when sizing grids beside the sidebar. */
export const LAYOUT = {
    SIDEBAR_WIDTH: 64,
} as const;