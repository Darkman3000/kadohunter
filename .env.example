# Backend / workspace variables.
# Copy to a real .env file in the workspace root.

PORT=4000
WEB_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Auth
CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
CLERK_WEBHOOK_SECRET=
# Convex auth.config.ts — Frontend API URL (https://YOUR_INSTANCE.clerk.accounts.dev)
CLERK_JWT_ISSUER_DOMAIN=

# Data layer
CONVEX_DEPLOYMENT=
CONVEX_URL=

# Hosting / ops
RAILWAY_PROJECT_ID=
RAILWAY_ENVIRONMENT_NAME=
HOSTINGER_TOKEN=