# Product Requirements Document - `{{PROJECT_NAME}}`

> Purpose: define exactly what `{{PROJECT_NAME}}` is building before architecture work expands.  
> Rule: if a feature is not in this PRD, it is not part of the MVP.

## 1. Product Definition

`{{PROJECT_NAME}}` is a `{{PRODUCT_CATEGORY}}` product for `{{TARGET_USER}}`.

## 2. Problem

`{{CORE_PROBLEM}}`

## 3. Target User

Primary user:

- `{{TARGET_USER}}`

Primary user story:

- As a `{{TARGET_USER_ROLE}}`, I want to `{{PRIMARY_USER_GOAL}}`, so I can `{{PRIMARY_USER_OUTCOME}}`.

## 4. Core Promise

`{{PROJECT_NAME}}` should make `{{CORE_PROMISE}}`.

The first user-facing promise is:

- `{{PROMISE_STEP_1}}`
- `{{PROMISE_STEP_2}}`
- `{{PROMISE_STEP_3}}`
- `{{PROMISE_STEP_4}}`

## 5. MVP Scope

### P0 - Must Have

- `{{P0_ITEM_1}}`
- `{{P0_ITEM_2}}`
- `{{P0_ITEM_3}}`
- `{{P0_ITEM_4}}`

### P1 - Should Have

- `{{P1_ITEM_1}}`
- `{{P1_ITEM_2}}`
- `{{P1_ITEM_3}}`

### P2 - Later

- `{{P2_ITEM_1}}`
- `{{P2_ITEM_2}}`
- `{{P2_ITEM_3}}`

## 6. Explicit Non-Goals For MVP

- `{{NON_GOAL_1}}`
- `{{NON_GOAL_2}}`
- `{{NON_GOAL_3}}`

## 7. User Journey

1. User lands in the app and understands the promise quickly.
2. User completes `{{ENTRY_FLOW}}`.
3. User signs in or creates an account.
4. User completes `{{CORE_INPUT_STEP_1}}`.
5. User completes `{{CORE_INPUT_STEP_2}}`.
6. System runs `{{CORE_PROCESS}}`.
7. User receives `{{CORE_RESULT}}`.
8. User saves or shares `{{SAVED_OUTPUT}}`.
9. User can return on another session or device and recover state.
10. User sees `{{MONETIZATION_OR_LIMIT_SURFACE}}` at the correct point in the funnel.

## 8. Default Technical Direction

- Web: `{{WEB_STACK}}`
- Mobile: `{{MOBILE_STACK}}`
- Backend: `{{BACKEND_STACK}}`
- Auth: `{{AUTH_PROVIDER}}`
- Data: `{{DATA_PROVIDER}}`
- Hosting: `{{HOSTING_PROVIDER}}`
- AI: `{{AI_PROVIDER}}`

## 9. Monetization Hypothesis

- Free tier: `{{FREE_TIER_RULE}}`
- Paid tier: `{{PAID_TIER_RULE}}`
- Upgrade trigger: `{{UPGRADE_TRIGGER}}`

## 10. Success Metrics

- Primary metric: `{{SUCCESS_METRIC}}`
- Secondary metric 1: `{{SECONDARY_METRIC_1}}`
- Secondary metric 2: `{{SECONDARY_METRIC_2}}`
- Secondary metric 3: `{{SECONDARY_METRIC_3}}`

## 11. Kill Criteria

- `{{KILL_CRITERIA_1}}`
- `{{KILL_CRITERIA_2}}`
- `{{KILL_CRITERIA_3}}`

## 12. Open Questions

- `{{OPEN_QUESTION_1}}`
- `{{OPEN_QUESTION_2}}`
- `{{OPEN_QUESTION_3}}`

## 13. Sign-Off Checklist

- [ ] Problem is clear
- [ ] User is clear
- [ ] MVP scope is locked
- [ ] Non-goals are explicit
- [ ] Metrics are defined
- [ ] Open questions are either answered or accepted